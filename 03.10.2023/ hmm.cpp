#include <iostream>
#include <vector>

int genN(int min, int max) {return rand() % (max - min + 1) + min;};

int main() {
    vec = std::vector<int>;

    while (true) {
        uint8_t num = genN(10, 25);


    }

    return 0;
}